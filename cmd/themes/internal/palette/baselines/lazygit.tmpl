# lazygit baseline — theme-independent gui config.
# The semantic block below appends `gui.theme.<key>` entries with
# concrete hex values.

gui:
  showFileTree: true
  showCommandLog: false
  showIcons: true
  nerdFontsVersion: "3"
