# fzf baseline — theme-independent options.
# The semantic block below appends `export FZF_DEFAULT_OPTS_HIGH_CONTRAST`
# with concrete color pairs.

# Layout / behavior defaults (theme-independent).
export FZF_DEFAULT_OPTS_LAYOUT="--layout=reverse --border --height=90%"
export FZF_DEFAULT_OPTS_PREVIEW="--preview-window=right:60%:wrap"
